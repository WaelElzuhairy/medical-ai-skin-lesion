# Secrets
.env

# Data and model artifacts (large, regenerate from scripts)
data/raw/
data/processed/
# chroma corpus is committed (29 MB) so HF Spaces has it at deploy time
models/checkpoints/
# Keep calibration JSONs (small), exclude logit caches (large)
models/calibration/*.npz
# Keep confusion matrices / reliability diagrams / metrics JSONs, exclude raw logit npz
models/reports/*.npz

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Streamlit
.streamlit/secrets.toml
